import 'package:flutter/material.dart';

class QuranSearchDebugScreen extends StatelessWidget {
  const QuranSearchDebugScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Quran Search (Debug)')),
      body: const Center(child: Text('Quran search debug placeholder screen.')),
    );
  }
}
