import 'package:flutter/material.dart';

class ListeningWarmupScreen extends StatelessWidget {
  const ListeningWarmupScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Listening Warmup')),
      body: const Center(child: Text('Listening warmup placeholder screen.')),
    );
  }
}
